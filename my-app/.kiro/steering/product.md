# Product

A browser-based notepad single-page application. Users can create, edit, and delete notes. All data is persisted in the browser's Local Storage (no backend). The interface uses a two-panel layout: note list on the left, editor on the right.
